export const metadata = {
  title: 'Email Preferences | diBoaS',
  robots: { index: false, follow: false },
};

export default function EmailPreferencesLayout({ children }: { children: React.ReactNode }) {
  return children;
}
