/**
 * Investor domain — request capture for the investor vertical.
 *
 * Public surface consumed by the API route. Store helpers + value types stay
 * internal to the module (imported directly where needed).
 */

export { investorRequestService } from './InvestorRequestService';
export { isInvestorType } from './types';
