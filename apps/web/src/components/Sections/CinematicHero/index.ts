/**
 * CinematicHero — reusable GSAP + Three.js animated hero family.
 * Barrel export.
 */

export { CinematicHeroFactory, default } from './CinematicHeroFactory';
export type {
  CinematicHeroProps,
  CinematicHeroCta,
  SceneKind,
  HeroTheme,
} from './types';
