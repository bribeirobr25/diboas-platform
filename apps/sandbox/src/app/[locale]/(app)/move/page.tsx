import { MoneyOut } from '@/components/MoneyOut';

/**
 * Move / money-out (R5; W-9c). The "Move" tab. Money families are present but
 * disabled in practice mode with honest explainer sheets.
 */
export default function MovePage() {
  return <MoneyOut />;
}
