import { PracticeRecord } from '@/components/PracticeRecord';

/**
 * Practice record (R3; W-11). Inside the app shell. Milestones project from
 * product events (deferred); public-visibility persists with the account model.
 */
export default function PracticeRecordPage() {
  return <PracticeRecord />;
}
