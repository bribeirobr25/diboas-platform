{
  "name": "diboas-platform",
  "version": "1.0.0",
  "description": "diBoaS Platform - Unified Financial Services",
  "private": true,
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean",
    "validate:design-tokens": "node scripts/validate-design-tokens.js",
    "generate:design-tokens": "node scripts/validate-design-tokens.js && node scripts/generate-design-tokens.js",
    "dev:web": "turbo run dev --filter=web",
    "dev:app": "turbo run dev --filter=app",
    "dev:business": "turbo run dev --filter=business",
    "dev:clean": "lsof -ti:3000 | xargs kill 2>/dev/null; pnpm -w run dev:web",
    "dev:reset": "rm -rf apps/web/.next .turbo && pnpm -w run dev:web",
    "dev:fresh": "rm -rf apps/web/.next .turbo && pnpm --filter @diboas/i18n build && pnpm -w run dev:web",
    "performance:audit": "turbo run lighthouse",
    "accessibility:audit": "turbo run pa11y",
    "security:audit": "pnpm audit --audit-level moderate",
    "analytics:report": "turbo run analytics-report",
    "audit:full": "bash scripts/pre-launch-audit.sh",
    "audit:ci": "bash scripts/pre-launch-audit.sh --ci"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "ajv": "^8.17.1",
    "eslint": "^9.39.2",
    "prettier": "^3.7.4",
    "turbo": "^2.7.3",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "^0.25.0",
      "js-yaml": "^4.1.1",
      "glob": "^10.5.0",
      "next@<15.5.8": ">=15.5.8",
      "tmp@<=0.2.3": ">=0.2.4",
      "qs@<6.14.1": ">=6.14.1",
      "preact@<10.28.2": ">=10.28.2"
    }
  }
}